CreateCollection({ name: "users" });
CreateIndex(
	{
		name: "users_by_name",
		source: Collection("users"),
		terms: [{ field: ["data", "name"] }]
	});
CreateIndex(
	{
		name: "all_users",
		source: Collection("users")
	});
